<!DOCTYPE html>
<html>
    <head>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
        <meta charset="utf-8">
        <title>Registrar's Office</title>
    </head>
    <body>
        <div class="jumbotron container">
            <h2 style="text-align:center;">Anand's School of Coding and Wizardry</h2><hr>
                <div class="row">
                    <div class="col-sm-6">
                        <h3>Current Courses:</h3>
                        <form action="/create_course" method="post">
                            <div class="form-group">
                                <label for="name">New Course Name:</label>
                                <input type="text" name="name" class="form-control" required>
                                <label for="number">New Course Number:</label>
                                <input type="text" name="number" class="form-control" required>
                            </div>
                            <button type="submit" class="btn btn-primary">Create New Course</button>
                        </form><hr>
                        {% for course in courses %}
                            <li><a href="/course/{{course.getId}}">{{course.getNumber}}: {{course.getName}}</a></li>
                        {% endfor %}<br>
                        <form action="/delete_all_courses" method="post">
                            <button type="submit" class="btn btn-danger">DELETE ALL COURSES</button>
                        </form>
                    </div>
                    <div class="col-sm-6">
                        <h3>Current Students:</h3>
                        <form action="/create_student" method="post">
                            <div class="form-group">
                                <label for="name">New Student Name:</label>
                                <input type="text" name="name" class="form-control" required>
                                <label for="enrollment_date">Enrollment:</label>
                                <input type="date" name="enrollment_date" class="form-control" required>
                            </div>
                            <button type="submit" class="btn btn-success">Create New Student</button>
                        </form><hr>
                        {% for student in students %}
                            <li><a href="/student/{{student.getId}}">{{student.getName}}</a>: enrolled on {{student.getEnrollment}}</li>
                        {% endfor %}<br>
                        <form action="/delete_all_students" method="post">
                            <button type="submit" class="btn btn-danger">DELETE ALL STUDENTS</button>
                        </form>
                    </div>
                </div>

        </div>

    </body>
</html>
