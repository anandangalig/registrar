<!DOCTYPE html>
<html>
    <head>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
        <meta charset="utf-8">
        <title>Update Course</title>
    </head>
    <body>
        <div class="container">
            <h1> Update the <em>{{course.getName}}</em> course:</h1>
            <form action="/courses/{{course.getId}}" method="post">
                <input name="_method" type="hidden" value="patch">
                <label for="new_course_name">Rename this Course:</label>
                <input name="new_course_name" type="text" class="form-control"><br>
                <button type="submit" class="btn btn-success">Update</button>
            </form><hr>
            <form action="/delete_course/{{course.getId}}" method="post">
                <button type="submit" class="btn btn-danger">DELETE</button>
            </form>
        </div>
    </body>
</html>
